version: 2.1

executors:
  my-executor:
    docker:
      - image: gcc

jobs:
  test:
    executor: my-executor
    steps:
      - checkout
      - run: make test
      
  build:
    executor: my-executor
    steps:
      - checkout
      - run: make
